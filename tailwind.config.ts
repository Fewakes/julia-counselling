import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  // Tailwind v4 no longer needs content globs
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
